import { Outfit, Oleo_Script_Swash_Caps } from 'next/font/google';

export const outfit = Outfit({ subsets: ['latin'] });
export const oleo = Oleo_Script_Swash_Caps({
  subsets: ['latin'],
  weight: ['400'],
});
